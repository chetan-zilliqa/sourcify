{
  "name": "sourcify-database",
  "version": "2.7.2",
  "private": true,
  "description": "The Sourcify database",
  "main": "index.js",
  "scripts": {
    "sourcify:database": "node scripts.mjs ",
    "migrate:setup": "rm -rf migrations-temp && mkdir -p migrations-temp && ln -sf $PWD/database-specs/migrations/* $PWD/migrations-temp/ && ln -sf $PWD/migrations/* $PWD/migrations-temp/",
    "migrate:up": "npm run migrate:setup && DBMATE_MIGRATIONS_DIR=./migrations-temp dbmate up",
    "migrate:rollback": "npm run migrate:setup && DBMATE_MIGRATIONS_DIR=./migrations-temp dbmate rollback",
    "migrate:new": "DBMATE_MIGRATIONS_DIR=./migrations dbmate new",
    "migrate:create-db": "dbmate create",
    "migrate:drop-db": "dbmate drop",
    "migrate:load-schema": "dbmate load",
    "migrate:status": "npm run migrate:setup && DBMATE_MIGRATIONS_DIR=./migrations-temp dbmate status",
    "massive-replace": "ts-node massive-replace-script/massive-replace-script.ts"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "12.1.0",
    "dbmate": "2.28.0",
    "dotenv": "16.6.1",
    "ethers": "6.15.0",
    "pg": "8.16.3",
    "readdirp": "3.6.0"
  }
}
